<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="css/check_style.css">
    
</head>
<body>
    
    <h1 id = "game"><a href="/" style="color: rgb(131, 79, 79)">konek.io</a></h1>

    <h2 id = "player-name">Player: <%= player_name %> </h2>

    <h2 id = "round-count">Round: <%= round %></h2>
    
    <h1 id = "list-title">Word Link</h1>

    <ul id = "word-list">
        <% words.forEach((word) =>{ %>
            <li id = "<%= word.toLowerCase().trim()%>"> <%= word %> </li>

        <% }); %> 

    </ul>

    <div class="line"></div>

    <div id = "cancel-prompt">

        <h1 id = "cancel-title">Enter the word to cancel out: </h1>
        <input id = "input-prompt" type="text"></input>
        <input id = "submit" onclick="cancel_word()"></input>

    </div>

    <h2 id = "show-score">Total Score: <%= score%></h2>

    <a href="<%= continue_path%> " id = "continue-end" onclick="submit_score()">Continue</a>

    <h1 id = "caution"></h1>

    <script src = "scripts/check_page.js"></script>


</body>
</html>